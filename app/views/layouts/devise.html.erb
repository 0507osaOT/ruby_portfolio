<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Ruby Portfolio" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
    <%= stylesheet_link_tag "devise" %>
  </head>

  <body>
    <% if notice %>
      <div class="alert alert-notice"><%= notice %></div>
    <% end %>
    <%# 特定の「ログインまたは登録が必要です。」メッセージは表示しない %>
    <% unauth_msg = I18n.t("devise.failure.unauthenticated", default: nil) %>
    <% if alert.present? && alert != unauth_msg %>
      <div class="alert alert-alert"><%= alert %></div>
    <% end %>

    <%= yield %>
  </body>
</html>